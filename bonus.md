# Bonus - Local Clone

I cloned the repository using this command:

`git clone https://github.com/USERNAME/Git-Research-Assignment.git`

Then I added this file, committed, and pushed with:

`git add bonus.md`  
`git commit -m "Add bonus file"`  
`git push origin main`

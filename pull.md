# Pull

The `git pull` command fetches the latest changes from a remote repository and automatically merges them into your current local branch. It is essentially a combination of `git fetch` followed by `git merge`.

Source:  
https://git-scm.com/docs/git-pull
